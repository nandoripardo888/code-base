# Indexing roadmap

Indexing is not part of the first lexical release. Phase two will add SQLite as
the source of truth, explicit migrations, SHA-256 change detection, FTS, index
runs, integrity status, and incremental updates. Direct Ripgrep search and source
reading will remain available when the index is absent or unhealthy.
